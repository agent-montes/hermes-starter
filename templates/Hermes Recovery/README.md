# Hermes Recovery

Private-only. Do not publish real recovery contents.

This template folder contains only blank restore and backup manifest templates.

In a live setup, Recovery may contain encrypted backups, restore manifests, verification notes, and machine replacement instructions.

Never commit real backup archives, token-bearing config snapshots, session databases, request dumps, channel routing data, or secret-bearing manifests.
