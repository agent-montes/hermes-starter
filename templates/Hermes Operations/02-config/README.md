# Config Lane

Document non-secret configuration.

Include:
- config file locations using placeholders
- provider/model names without keys
- tool enablement notes
- restart/new-session expectations

Secret values belong in Secrets, not here.
